#!/usr/bin/env nu

def backend [] {
    try { $env.XDG_BACKEND } catch { 'wsl' }
}

# 粘贴剪贴板内容至输出
def main [] {
    $in | match (backend) {
        'wayland' => { wl-copy },
        'x11' => { xclip -sel c },
        _ => { clip.exe },
    }
}
